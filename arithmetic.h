#define PI 3.141593
#define E 2.718282

//finished
//adds together 2 unsigned integers.
unsigned long uadd(unsigned, unsigned);

//finished
//subtracts 2 unsigned integers.
unsigned usub(unsigned, unsigned);

//finished
//adds 2 floats
float add(float, float);

//finished
//subtracts 2 floats
float sub(float, float);

//bugged (1.5*1.5 = 1.0)
//multiplies 2 floats
float mult(float, float);

//finished
//divides 2 floats
float mydiv(float, float);

//finished
//gets the n power of a float
float ipow(float, int);

//wip (debug mult first)
//gets the square root of a number.
float mysqrt(float);
#include "arithmetic.c"
